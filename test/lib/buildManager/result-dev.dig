_export:
  td:
    database: 'dev-db'
    table: 'dev-table'
    result_table: 'result-dev-table'

+task:
  td>: 'SELECT name, SUM(score) FROM ${td.table} GROUP BY 1'
  insert_into: ${td.result_table}
  database: ${td.database}
